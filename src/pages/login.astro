---
import { LoginButtonWrapper } from '@/features/auth/components/LoginButtonWrapper';
import '../styles/global.css';
---

<html lang="es">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Iniciar Sesión - Diputados Guatemala</title>
    <meta name="description" content="Inicia sesión para acceder al sistema de información de diputados" />
  </head>
  <body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
    <main class="container mx-auto px-4 py-16 flex items-center justify-center min-h-screen">
      <div class="w-full max-w-md">
        <div class="bg-white rounded-2xl shadow-xl p-8 md:p-12">
          <div class="text-center mb-8">
            <div class="mb-6">
              <span class="text-6xl">🏛️</span>
            </div>
            <h1 class="text-3xl font-bold text-gray-900 mb-2">
              Bienvenido
            </h1>
            <p class="text-gray-600">
              Inicia sesión para acceder al sistema
            </p>
          </div>

          <div class="flex flex-col gap-4">
            <LoginButtonWrapper client:load />
          </div>

          <div class="mt-8 text-center text-sm text-gray-500">
            <p>Sistema de información</p>
            <p>Congreso de la República de Guatemala</p>
          </div>
        </div>
      </div>
    </main>
  </body>
</html>
